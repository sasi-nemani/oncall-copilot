# Core: provider SDKs + MCP server
openai>=1.40
anthropic>=0.40
mcp>=1.0

# Loads a local .env automatically (optional but recommended; falls back to plain env vars)
python-dotenv>=1.0

# Optional extra: local embeddings for semantic/hybrid retrieval (RETRIEVAL_MODE=hybrid).
# NOT needed for the default keyword retriever; pulls in torch (heavy). Install with:
#   pip install sentence-transformers
# sentence-transformers>=2.2

# Optional extra: OpenTelemetry tracing (OTEL_ENABLED=1). Code no-ops without it. Install with:
#   pip install opentelemetry-sdk
# opentelemetry-sdk>=1.20
